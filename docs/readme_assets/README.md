# README example images

- **GradCAM** panels: copied from `grad_cam/cam_results/` after running `grad_cam/02_yolo_cam.py` (detection + `cam_overlay.png`).
- **Missed case**: `image751_annotated.jpg` from `grad_cam/failure_bank/annotated/` (or parent `failure_bank/annotated/`).

Regenerate by re-running the pipeline and copying the same stems, or replace with your own samples.
